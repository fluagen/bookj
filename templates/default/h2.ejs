<ul class="nav">
	<%
	for(var i=0; i<rst.length; i++){
	var obj = rst[i];
	%>
	<li role="presentation">
		<a href="#<%= obj.id %>"><%= obj.title%></a>
		<%
		if(obj.subs.length > 0){
		%>
		<%- include('h3', {rst_h3: obj.subs}) %>
		<%}%>
	</li>
	<%
	}
	%>
</ul>